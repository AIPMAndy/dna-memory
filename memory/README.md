# Runtime data

This directory is intentionally empty in source control. DNA Memory databases,
events, genes, capsules, and other user-generated memory data are local runtime
artifacts and must never be committed.

The recommended database location is outside the repository:
`~/.local/share/dna-memory/memory.db`.
